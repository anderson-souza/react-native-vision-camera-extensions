import { AngleLine } from '../index';

describe('react-native-vision-camera-extensions', () => {
  it('exports AngleLine component', () => {
    expect(AngleLine).toBeDefined();
    expect(typeof AngleLine).toBe('function');
  });
});
